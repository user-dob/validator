import { ValidateDecoratorMetadataItem } from '../interfaces';

export class ValidatePropertyStrategy {

    constructor(
        private readonly metadata: ValidateDecoratorMetadataItem
    ) { }

        

}